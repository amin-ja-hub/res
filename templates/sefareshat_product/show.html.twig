{% extends 'base.html.twig' %}

{% block title %}SefareshatProduct{% endblock %}

{% block body %}
    <h1>SefareshatProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sefareshat_product.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sefareshat_product_index') }}">back to list</a>

    <a href="{{ path('app_sefareshat_product_edit', {'id': sefareshat_product.id}) }}">edit</a>

    {{ include('sefareshat_product/_delete_form.html.twig') }}
{% endblock %}
